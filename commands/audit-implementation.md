Audit the implementation above against the plan that was used to produce it. Before marking anything complete:
 
1. Read the actual implementation of every file, function, and module the plan required
2. Verify the implementation matches the plan — check signatures, types, logic, exports, and edge case handling against what was specified
3. Identify any gaps between what the plan required and what was actually built — missing pieces, incorrect assumptions carried forward, partial implementations, or logic that diverges from the spec
4. For each gap found, state clearly: what the plan required, what was built instead, and the likely impact
5. Produce a verdict: fully implemented, partially implemented (list what's missing), or incorrectly implemented (list what diverged) — then confirm with the user before any remediation begins
